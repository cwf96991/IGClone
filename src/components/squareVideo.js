const SquareVideo = () => {
  return (
    <div className={`row-span-2 col-span-2 overflow-hidden relative pt-[100%]`}>
      {/* <PostHover post={post}>

        <ReactPlayer
          url={video}
          height={"auto"}
          width="100%"
          className={`absolute right-0 bottom-0 object-cover object-center  `}
        />
      </PostHover> */}
    </div>
  );
};
